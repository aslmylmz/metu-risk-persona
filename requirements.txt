# Python dependencies for METU Risk Persona
# Install with: pip install -r requirements.txt

# Core scientific stack
numpy>=1.26
scipy>=1.12
pandas>=2.2

# Machine learning
scikit-learn>=1.4

# Visualization (clustering pipeline)
matplotlib>=3.8

# Schema validation (scoring engine)
pydantic>=2.6

# Optional: FastAPI for serving the scoring engine
# fastapi>=0.110
# uvicorn[standard]>=0.29
