\documentclass[11pt,a4paper]{article}

% ── Packages ──────────────────────────────────────────────────────────────
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\usepackage[margin=1in]{geometry}
\usepackage{hyperref}
\usepackage{xcolor}
\usepackage{listings}
\usepackage{booktabs}
\usepackage{longtable}
\usepackage{array}
\usepackage{enumitem}
\usepackage{amsmath,amssymb}
\usepackage{parskip}
\usepackage{fancyhdr}
\usepackage{titlesec}
\usepackage{caption}
\usepackage{float}
\usepackage{graphicx}

% ── Colors ────────────────────────────────────────────────────────────────
\definecolor{codebackground}{HTML}{F5F5F5}
\definecolor{codekeyword}{HTML}{005CC5}
\definecolor{codecomment}{HTML}{6A737D}
\definecolor{codestring}{HTML}{032F62}
\definecolor{purpleballoon}{HTML}{A855F7}
\definecolor{tealballoon}{HTML}{14B8A6}
\definecolor{orangeballoon}{HTML}{F97316}

% ── Hyperref setup ────────────────────────────────────────────────────────
\hypersetup{
    colorlinks=true,
    linkcolor=blue!60!black,
    urlcolor=blue!60!black,
    citecolor=blue!60!black,
    bookmarksdepth=3
}

% ── Listings style ────────────────────────────────────────────────────────
\lstdefinestyle{pythonstyle}{
    language=Python,
    backgroundcolor=\color{codebackground},
    basicstyle=\ttfamily\small,
    keywordstyle=\color{codekeyword}\bfseries,
    commentstyle=\color{codecomment}\itshape,
    stringstyle=\color{codestring},
    breaklines=true,
    frame=single,
    rulecolor=\color{black!20},
    xleftmargin=1em,
    xrightmargin=1em,
    aboveskip=1em,
    belowskip=1em,
    showstringspaces=false,
    columns=flexible,
    morekeywords={np,stats,float,int,dict,list,tuple,bool,defaultdict}
}

\lstdefinestyle{tsstyle}{
    language=Java,
    backgroundcolor=\color{codebackground},
    basicstyle=\ttfamily\small,
    keywordstyle=\color{codekeyword}\bfseries,
    commentstyle=\color{codecomment}\itshape,
    stringstyle=\color{codestring},
    breaklines=true,
    frame=single,
    rulecolor=\color{black!20},
    xleftmargin=1em,
    xrightmargin=1em,
    aboveskip=1em,
    belowskip=1em,
    showstringspaces=false,
    columns=flexible,
    morekeywords={const,let,Math,if,return}
}

\lstdefinestyle{jsonstyle}{
    backgroundcolor=\color{codebackground},
    basicstyle=\ttfamily\scriptsize,
    breaklines=true,
    frame=single,
    rulecolor=\color{black!20},
    xleftmargin=1em,
    xrightmargin=1em,
    aboveskip=1em,
    belowskip=1em,
    showstringspaces=false,
    columns=flexible
}

% ── Header / Footer ──────────────────────────────────────────────────────
\pagestyle{fancy}
\fancyhf{}
\fancyhead[L]{\small BART Metrics Reference --- Full Technical Documentation}
\fancyhead[R]{\small\thepage}
\renewcommand{\headrulewidth}{0.4pt}

% ── Title ─────────────────────────────────────────────────────────────────
\title{\textbf{BART Metrics Reference}\\[0.3em]
\large Full Technical Documentation}
\author{Ahmet Selim Y{\i}lmaz}
\date{}

% ══════════════════════════════════════════════════════════════════════════
\begin{document}
\maketitle
\thispagestyle{fancy}

\tableofcontents
\newpage

% ──────────────────────────────────────────────────────────────────────────
\section{Explosion Model}
% ──────────────────────────────────────────────────────────────────────────

The original BART by Lejuez et al.\ (2002) works by pre-drawing a single breakpoint
from a uniform distribution and popping the balloon deterministically the moment
that threshold is hit. Our version does something different. We went with a
\textbf{sequential independent Bernoulli model} instead, where on each pump
attempt $k$ the balloon has a chance of blowing up given by

\begin{equation}
\label{eq:explosion}
P(\text{explode at pump } k) = \frac{k}{\texttt{maxPumps}}\,.
\end{equation}

On the client side, the check looks like this:

\begin{lstlisting}[style=tsstyle,caption={Explosion check (TypeScript, client-side).}]
const newPumps = currentBalloon.pumps + 1;
const explosionProbability = newPumps / maxPumps;
const explode = newPumps >= maxPumps
             || Math.random() < explosionProbability;
\end{lstlisting}

There are two things worth noting here.
First, because there is no hidden breakpoint drawn ahead of time, each individual
pump is a genuinely random event---the balloon will always pop once you reach
\texttt{maxPumps}, but until then every press of the button is its own coin flip.
Second, and maybe less obvious, the expected-value-optimal place to stop turns out
to be well below $\texttt{maxPumps}/2$:

\begin{table}[H]
\centering
\begin{tabular}{@{}llccc@{}}
\toprule
\textbf{Color} & \textbf{Hex} & \textbf{\texttt{maxPumps}} & \textbf{Risk Level} & \textbf{EV-Optimal Stop} \\
\midrule
\textcolor{purpleballoon}{Purple} & \texttt{\#A855F7} & 128 & Low    & ${\sim}\,12$ pumps \\
\textcolor{tealballoon}{Teal}     & \texttt{\#14B8A6} & 32  & Medium & ${\sim}\,6$ pumps  \\
\textcolor{orangeballoon}{Orange} & \texttt{\#F97316} & 8   & High   & ${\sim}\,2$ pumps  \\
\bottomrule
\end{tabular}
\caption{Balloon color profiles. We picked neutral colors on purpose to
steer clear of common psychological associations (e.g.\ red\,$=$\,danger).}
\end{table}

\subsection{Session Generation}

Each session consists of exactly 30 balloons---10 of each color---presented in a
random order. The shuffling is done via Fisher--Yates so that no predictable
sequence leaks through:

\begin{lstlisting}[style=tsstyle,caption={Shuffle logic (TypeScript).}]
["ORANGE", "TEAL", "PURPLE"].forEach((type) => {
    const profile = RISK_PROFILES[type];
    for (let i = 0; i < 10; i++)
        configs.push({ id: 0, color: profile.color,
                       maxPumps: profile.maxPumps });
});
for (let i = configs.length - 1; i > 0; i--) {
    const j = Math.floor(Math.random() * (i + 1));
    [configs[i], configs[j]] = [configs[j], configs[i]];
}
\end{lstlisting}


% ──────────────────────────────────────────────────────────────────────────
\section{Raw Event Data Format}
% ──────────────────────────────────────────────────────────────────────────

Every user action gets logged as a timestamped JSON object on the client.
We rely on \texttt{performance.now()} for timestamps (it is monotonic and
sub-millisecond) rather than \texttt{Date.now()}, which can jump around if
the system clock is adjusted mid-session. Each event carries three fields:
\texttt{type}, \texttt{timestamp}, and \texttt{payload}.

\begin{lstlisting}[style=jsonstyle,caption={A handful of raw events from the sample session (\texttt{6fe2f79a}).}]
{"type":"pump",   "timestamp":36678.70, "payload":{"color":"purple","balloon_id":1}}
{"type":"pump",   "timestamp":37262.10, "payload":{"color":"purple","balloon_id":1}}
  ...  (4 more pumps)
{"type":"collect","timestamp":40844.90, "payload":{"color":"purple","balloon_id":1}}
{"type":"pump",   "timestamp":42964.50, "payload":{"color":"teal",  "balloon_id":2}}
{"type":"pump",   "timestamp":43418.00, "payload":{"color":"teal",  "balloon_id":2}}
{"type":"pump",   "timestamp":43836.20, "payload":{"color":"teal",  "balloon_id":2}}
{"type":"explode","timestamp":43836.30, "payload":{"color":"teal",  "balloon_id":2,
                                                    "pump_count":3}}
\end{lstlisting}

The scoring engine's first job is to chop this flat stream into per-balloon groups.
A new group begins right after every \texttt{collect} or \texttt{explode} event:

\begin{lstlisting}[style=pythonstyle,caption={Balloon segmentation (Python).}]
def _segment_balloons(events):
    balloons, current = [], []
    for event in events:
        current.append(event)
        if event.type in ("collect", "explode"):
            balloons.append(current)
            current = []
    if current:
        balloons.append(current)
    return balloons
\end{lstlisting}

Running this on our sample session produces 30 balloon segments---10~purple,
10~teal, 10~orange---as expected.


% ──────────────────────────────────────────────────────────────────────────
\section{Handling Right-Censored Data}
% ──────────────────────────────────────────────────────────────────────────

Whenever a balloon explodes, the pump count we observe is truncated; the
participant might very well have kept going if the balloon had survived.
This is essentially a right-censoring problem, and sweeping it under the rug
biases every mean downward.

To make this concrete, consider the orange balloons in our sample session. All 10
of them blew up. Their observed counts ($1, 4, 5, 2, 4, 4, 4, 2, 2, 1$)
average out to $2.9$, but those numbers are more about where the RNG happened to
cut each trial short than about the participant's actual stopping rule. With zero
collected orange balloons, we really cannot say what the person \emph{intended}
to do.

Our solution is simple: any time we want to measure \emph{what someone chose to do},
we only look at \textbf{collected (non-exploded) balloons}. A small helper function
handles this and falls back to all balloons (with a warning flag) when there are
fewer than two collected trials for a particular color:

\begin{lstlisting}[style=pythonstyle,caption={Collected-preference helper (Python).}]
MIN_COLLECTED_FALLBACK = 2

def _prefer_collected(collected, all_data, min_count=2):
    if len(collected) >= min_count:
        return collected, False      # use collected
    return all_data, True            # fallback + warning
\end{lstlisting}

For orange, this fallback fires pretty routinely---the probability of surviving even
4 pumps on an orange balloon is only about $16\%$, so in most sessions you end up with
very few (or zero) collected orange balloons.


% ──────────────────────────────────────────────────────────────────────────
\section{Overall Metrics}
% ──────────────────────────────────────────────────────────────────────────

\subsection{\texttt{average\_pumps\_adjusted}}

This is the classic ``adjusted'' BART score from Lejuez's original work: the mean
number of pumps across only those balloons that the participant chose to cash in.

\begin{lstlisting}[style=pythonstyle]
non_exploded_pumps = []
for balloon_events in balloons:
    pumps = sum(1 for e in balloon_events if e.type == "pump")
    terminal = next(
        (e.type for e in reversed(balloon_events)
         if e.type in ("collect", "explode")), None)
    if terminal != "explode":
        non_exploded_pumps.append(pumps)

average_pumps_adjusted = (np.mean(non_exploded_pumps)
                          if non_exploded_pumps
                          else np.mean(all_pumps))
\end{lstlisting}

The range is $[0,\, 128]$.  In the sample session, 10~balloons were collected
with counts $\{6, 6, 5, 5, 11, 7, 3, 21, 5, 3\}$, giving
$\texttt{average\_pumps\_adjusted} = 7.2$.

An important limitation: this number treats all colors the same. Someone who
pumps 5~times on every color and someone who pumps 20~times on purple but only
once on orange could end up looking identical. If you want something that
accounts for the color differences, see \texttt{rng\_normalized\_pumps}
(\S\ref{sec:rng_norm}) or \texttt{risk\_adjustment\_score} (\S\ref{sec:risk_adj}).


\subsection{\texttt{avg\_pumps\_all\_balloons}}

The plain average over every balloon, whether it was collected or not:

\begin{equation}
    \texttt{avg\_pumps\_all\_balloons} = \frac{1}{N}\sum_{i=1}^{N} \text{pumps}_i
\end{equation}

For the sample session this comes out to $168 / 30 = 5.6$. It is handy for
direct comparisons with other BART studies that report unadjusted means, though
it does understate what the participant was trying to do since exploded balloons
carry truncated counts.


\subsection{\texttt{explosion\_rate}}

Just the fraction of balloons that popped:

\begin{equation}
    \texttt{explosion\_rate} = \frac{\texttt{total\_explosions}}{\texttt{total\_balloons}}
\end{equation}

In our sample session that is $20/30 = 0.667$. Keep in mind that two people
playing exactly the same strategy can end up with noticeably different explosion
rates on any given run---the task is stochastic, after all.


\subsection{\texttt{mean\_latency\_between\_pumps}}

The average number of milliseconds between consecutive pumps \emph{within} the
same balloon. We throw out cross-balloon gaps (the time someone spends deciding
whether to start a new balloon) and we also filter out any intra-balloon pause
longer than 2\,s, which we treat as an outlier.

\begin{lstlisting}[style=pythonstyle]
all_intra_latencies = []
for balloon_events in balloons:
    pump_times = [e.timestamp for e in balloon_events
                  if e.type == "pump"]
    if len(pump_times) >= 2:
        diffs = np.diff(pump_times)
        all_intra_latencies.extend(diffs.tolist())

latencies = np.array(all_intra_latencies)
latencies = latencies[latencies < 2000.0]
mean_latency = np.mean(latencies)
\end{lstlisting}

As an example, here are the inter-pump intervals for balloon~1
(purple, collected at 6~pumps):
\begin{align*}
\text{Timestamps} &= [36678.7,\; 37262.1,\; 38028.8,\; 38628.6,\; 39362.1,\; 40012.1] \\
\text{Diffs (ms)} &= [583.4,\; 766.7,\; 599.8,\; 733.5,\; 650.0]
\end{align*}
Those five intervals go into the global pool. Across all 30 balloons in the
session, the overall mean turns out to be $555.4$\,ms---fairly deliberate.
Fast or impulsive pumpers typically sit somewhere around ${\sim}200$\,ms.


\subsection{Session-Level Counts}

The fields \texttt{total\_balloons}, \texttt{total\_pumps},
\texttt{total\_explosions}, and \texttt{total\_collections} are
straightforward tallies. They should always obey

\begin{equation}
    \texttt{total\_explosions} + \texttt{total\_collections} = \texttt{total\_balloons}
\end{equation}

Sample session: $20 + 10 = 30$\;\checkmark.


% ──────────────────────────────────────────────────────────────────────────
\section{Per-Color Metrics}
% ──────────────────────────────────────────────────────────────────────────

For each color we produce a \texttt{ColorMetrics} object.  The color label
is pulled from the event payload, defaulting to teal if none is found:

\begin{lstlisting}[style=pythonstyle]
def _extract_balloon_color(balloon_events):
    for event in balloon_events:
        if hasattr(event.payload, "color") and event.payload.color:
            return event.payload.color.lower()
    return "teal"
\end{lstlisting}

\begin{table}[H]
\centering
\begin{tabular}{@{}lp{8.5cm}@{}}
\toprule
\textbf{Field} & \textbf{Description} \\
\midrule
\texttt{color}                  & \texttt{"purple"}, \texttt{"teal"}, or \texttt{"orange"} \\
\texttt{average\_pumps}         & Mean pumps across \emph{all} balloons of this color \\
\texttt{behavioral\_avg\_pumps} & Mean pumps from collected balloons only (falls back to all if $< 2$ collected) \\
\texttt{explosion\_rate}        & Color-specific explosion rate \\
\texttt{total\_balloons}        & How many balloons of this color appeared (expected: 10) \\
\texttt{collected\_count}       & How many of those survived to collection \\
\texttt{risk\_profile}          & \texttt{"low"} / \texttt{"medium"} / \texttt{"high"} \\
\texttt{used\_fallback}         & True when \texttt{behavioral\_avg\_pumps} had to use the fallback \\
\bottomrule
\end{tabular}
\caption{Fields in the per-color \texttt{ColorMetrics} object.}
\end{table}

\begin{table}[H]
\centering
\begin{tabular}{@{}lccccc@{}}
\toprule
\textbf{Color} & \textbf{Balloons} & \textbf{Collected} & \textbf{Avg Pumps (all)} & \textbf{Explosion Rate} & \textbf{Fallback?} \\
\midrule
Purple & 10 & 5 & 9.9  & 0.50 & No \\
Teal   & 10 & 5 & 4.0  & 0.50 & No \\
Orange & 10 & 0 & 2.9  & 1.00 & Yes \\
\bottomrule
\end{tabular}
\caption{Color breakdown for the sample session.  Notice how the gap between
\texttt{behavioral\_avg\_pumps} and \texttt{average\_pumps} widens as the
explosion rate climbs---orange is the extreme case here.}
\end{table}


% ──────────────────────────────────────────────────────────────────────────
\section{Behavioral Intention Metrics}
% ──────────────────────────────────────────────────────────────────────────

Everything in this section tries to get at what the participant \emph{meant}
to do, so wherever possible we restrict ourselves to collected balloons.

\subsection{\texttt{rng\_normalized\_pumps}}
\label{sec:rng_norm}

This is our main risk-taking measure. For each collected balloon we divide the
pump count by that color's capacity, then average the whole lot:

\begin{equation}
    \texttt{rng\_normalized\_pumps} = \frac{1}{|\mathcal{C}|}\sum_{b \in \mathcal{C}}
        \frac{\text{pumps}_b}{\text{maxPumps}_{\text{color}(b)}}
\end{equation}

\begin{lstlisting}[style=pythonstyle]
normalized = []
for color, pumps_list in color_pumps_behavioral.items():
    cap = COLOR_PROFILES[color]["max_pumps"]
    for p in pumps_list:
        normalized.append(p / cap)

rng_normalized_pumps = np.mean(normalized)
\end{lstlisting}

This lives on $[0, 1]$. If someone scores $0.25$, they were using roughly
a quarter of each balloon's capacity on average. The normalization gets rid of
the scale difference between colors ($128$ versus $32$ versus $8$), which makes
this metric directly comparable across risk levels---something that
\texttt{average\_pumps\_adjusted} simply cannot do.


\subsection{\texttt{impulsivity\_index}}

This one zooms in on orange balloons specifically. We take the mean pump count
from collected orange trials and divide by the orange ceiling of~8:

\begin{equation}
    \texttt{impulsivity\_index} = \text{clip}\!\left(
        \frac{\text{mean}(\text{collected\_orange\_pumps})}{8},\; 0,\; 1\right)
\end{equation}

\begin{lstlisting}[style=pythonstyle]
orange_behavioral = color_pumps_behavioral.get("orange", [])
orange_avg = np.mean(orange_behavioral) if orange_behavioral else 0.0
impulsivity_index = np.clip(orange_avg / 8.0, 0.0, 1.0)
\end{lstlisting}

Collected orange balloons are uncommon---surviving 4 pumps happens only about
$16\%$ of the time---so when you do get one, it carries quite a bit of signal.
A score of $0.5$ tells you the participant was pushing to half of orange's
capacity, which is already well past the EV-optimal stopping point of around~2.

In the sample session every single orange balloon exploded, so the fallback
kicked in and we used all orange trials: $2.9 / 8 = 0.36$.
(The value of $1.0$ that appears in the attached JSON stems from an older formula
based on explosion counts; the definition above is the current one.)


\subsection{\texttt{patience\_index} and \texttt{patience\_index\_normalized}}

These are basically the mirror image of the impulsivity index. Here we look at
the mean pump count on collected purple balloons---the safest color, where the
smart move is to pump aggressively.

\begin{align}
    \texttt{patience\_index} &= \text{mean}(\text{collected\_purple\_pumps}) \\[2pt]
    \texttt{patience\_index\_normalized} &= \text{clip}\!\left(
        \frac{\texttt{patience\_index}}{128},\; 0,\; 1\right)
\end{align}

In the sample session we get $9.9$ (raw) and $0.077$ (normalized)---the
participant was not coming close to exploiting the headroom that purple balloons
offer.


\subsection{\texttt{orange\_avg\_pumps}}

This is just the raw version of \texttt{impulsivity\_index} before the
normalization step, so it lives on $[0, 8]$. Sometimes it is easier to reason
about in plain terms: ``this person pumped an average of 4~times on the
high-risk balloons.''


% ──────────────────────────────────────────────────────────────────────────
\section{Learning \& Adaptation Metrics}
% ──────────────────────────────────────────────────────────────────────────

Both metrics below are after the same thing: \emph{did the participant get
better over the course of the session?}  They just go about answering it
in different ways.

\subsection{\texttt{learning\_rate}}

We fit a simple OLS line through pump count versus trial number for each color,
using collected balloons only. The resulting slope is then weighted by $R^2$
so that a noisy fit does not contribute as much. Crucially, the ``right''
direction depends on the color: pumping \emph{more} on purple over time is
adaptive, whereas pumping \emph{less} on orange is.

\begin{lstlisting}[style=pythonstyle,caption={Regression-based learning rate (Python).}]
for color in color_trials_all:
    collected = color_trials_collected.get(color, [])
    all_t = color_trials_all[color]
    trials = collected if len(collected) >= 2 else all_t
    if len(trials) < 2:
        continue

    xs = np.array([t[0] for t in trials])
    ys = np.array([t[1] for t in trials])
    slope, _, r_value, _, _ = stats.linregress(xs, ys)
    weighted = slope * (r_value ** 2)

    if color == "orange":
        learning_slopes.append(-weighted)
    elif color == "purple":
        learning_slopes.append(weighted)
    else:                          # teal: half-weight
        learning_slopes.append(-weighted * 0.5)

learning_rate = np.clip(np.mean(learning_slopes), -1.0, 1.0)
\end{lstlisting}

The range is $[-1, 1]$. In our sample session it comes out to $0.059$.

The obvious weakness here is sample size. With only 10 trials per color---and
frequently fewer than that once you filter down to collected ones---a single
outlier trial can throw the regression off quite a bit.


\subsection{\texttt{half\_split\_learning\_rate}}

This is a more robust alternative that avoids regression entirely. For each color
we sort the collected trials by when they occurred, split them in half, and
then look at the relative change between the two halves:

\begin{equation}
    \delta_\text{color} = \frac{\bar{x}_{\text{2nd half}} - \bar{x}_{\text{1st half}}}
                                {\bar{x}_{\text{overall}}}
\end{equation}

\begin{lstlisting}[style=pythonstyle,caption={Half-split learning rate (Python).}]
for color in color_trials_all:
    trials = collected if len(collected) >= 4 else all_trials
    if len(trials) < 4:
        continue

    sorted_t = sorted(trials, key=lambda x: x[0])
    half = len(sorted_t) // 2
    m1 = np.mean([t[1] for t in sorted_t[:half]])
    m2 = np.mean([t[1] for t in sorted_t[half:]])
    m  = np.mean([t[1] for t in sorted_t])
    if m == 0:
        continue

    delta = (m2 - m1) / m
    if color == "orange":
        scores.append(-delta)
    elif color == "purple":
        scores.append(delta)
    else:
        scores.append(-delta * 0.5)

half_split_lr = np.clip(np.mean(scores), -1.0, 1.0)
\end{lstlisting}

A value of $0.2$ here means the participant's behavior improved by roughly
$20\%$ across the session. Because we are comparing group means instead of
fitting a line through a handful of scattered points, this version is
considerably less sensitive to individual outlier trials.


% ──────────────────────────────────────────────────────────────────────────
\section{Risk Calibration Metrics}
% ──────────────────────────────────────────────────────────────────────────

\subsection{\texttt{color\_discrimination\_index}}

This metric asks how clearly the participant told the safe balloons apart from the
dangerous ones. We measure the difference in pumping between purple
and orange as a normalized Cohen's~$d$:

\begin{equation}
    d = \frac{\bar{x}_{\text{purple}} - \bar{x}_{\text{orange}}}
             {s_{\text{pooled}}}
    \;,\qquad
    s_{\text{pooled}} = \sqrt{\frac{s^2_{\text{purple}} + s^2_{\text{orange}}}{2}}
\end{equation}

\begin{equation}
    \texttt{color\_discrimination\_index} = \text{clip}\!\left(\frac{d}{2},\; 0,\; 1\right)
\end{equation}

\begin{lstlisting}[style=pythonstyle]
purple_arr = np.array(color_pumps["purple"])
orange_arr = np.array(color_pumps["orange"])

mean_diff  = np.mean(purple_arr) - np.mean(orange_arr)
pooled_std = np.sqrt(
    (np.var(purple_arr, ddof=1) + np.var(orange_arr, ddof=1)) / 2)

if pooled_std == 0:
    return 1.0 if mean_diff > 0 else 0.0

cohens_d = mean_diff / pooled_std
discrimination = np.clip(cohens_d / 2.0, 0.0, 1.0)
\end{lstlisting}

We went with Cohen's~$d$ over a $t$-test because effect sizes do not depend
on sample size, and that matters quite a lot when $N$ per color can dip
as low as 2.

In the sample session this comes out to $0.833$---the participant was clearly
pumping more on purple than on orange.


\subsection{\texttt{risk\_sensitivity}}

This takes a different angle. We compute the Pearson~$r$ between each balloon's
capacity ($128$/$32$/$8$) and the participant's actual pump count on that balloon.
The idea is to check whether the participant scaled their behavior in proportion
to the available headroom.

\begin{lstlisting}[style=pythonstyle]
capacities, pumps = [], []
for color, ps in color_pumps.items():
    cap = COLOR_PROFILES[color]["max_pumps"]
    for p in ps:
        capacities.append(cap)
        pumps.append(p)

r, _ = stats.pearsonr(capacities, pumps)
\end{lstlisting}

An $r$ above $0.7$ or so indicates strong sensitivity to the risk level;
$r \approx 0$ means a flat strategy that does not change across colors;
$r < 0$ would be unusual and would imply the person pumps \emph{more} on
the dangerous balloons. In the sample session we get $r = 0.678$.

One thing to keep in mind: a low $r$ is ambiguous on its own. Somebody who
deliberately plays the same way on every color will also produce $r \approx 0$.
Cross-referencing with \texttt{color\_discrimination\_index} helps sort out
whether the person genuinely cannot tell the colors apart or is just choosing
not to differentiate.


\subsection{\texttt{risk\_adjustment\_score}}
\label{sec:risk_adj}

Asks a slightly different question: not ``did you differentiate?'' but ``did you
pump the \emph{right amount} for each colour?''  Each colour is scored based on
its absolute distance from the EV-optimal point, and the three scores are averaged:

\begin{align}
    \text{Purple score}  &= \text{clip}\!\left(1 - \frac{|\bar{x}_\text{purple} - 12|}{116},\; 0,\; 1\right) \times 100
    &\quad&\text{(closer to 12\,$=$\,better)} \\[4pt]
    \text{Orange score}  &= \text{clip}\!\left(1 - \frac{|\bar{x}_\text{orange} - 2|}{6},\; 0,\; 1\right) \times 100
    &\quad&\text{(closer to 2\,$=$\,better)} \\[4pt]
    \text{Teal score}    &= \text{clip}\!\left(1 - \frac{|\bar{x}_\text{teal} - 6|}{26},\; 0,\; 1\right) \times 100
    &\quad&\text{(closer to 6\,$=$\,better)}
\end{align}

\begin{lstlisting}[style=pythonstyle]
optimal_stops = {"purple": 12.0, "teal": 6.0, "orange": 2.0}
max_pumps_caps = {"purple": 128, "teal": 32, "orange": 8}
scores = []

for color in ["purple", "teal", "orange"]:
    if color in cp and len(cp[color]) > 0:
        mean_pumps = np.mean(cp[color])
        opt = optimal_stops[color]
        mx = max_pumps_caps[color]
        
        # Max possible distance from optimal
        max_dist = max(opt, mx - opt)
        score = np.clip(1.0 - abs(mean_pumps - opt) / max_dist, 0.0, 1.0) * 100.0
        scores.append(score)

risk_adjustment_score = np.mean(scores) if scores else 0.0
\end{lstlisting}

The range is $[0, 100]$. In our view, this is the single best ``risk
intelligence'' number in the whole battery, because it penalizes both
over-pumping on dangerous balloons \emph{and} under-pumping on safe ones.
We swapped it in for \texttt{explosion\_rate} in the reflection pipeline
for exactly that reason---it captures what the participant actually chose,
not what the dice happened to roll.

Sample session: $32.2$---below-average calibration, pulled down mainly by the
participant's low exploitation of purple's generous headroom.


% ──────────────────────────────────────────────────────────────────────────
\section{Consistency Metrics}
% ──────────────────────────────────────────────────────────────────────────

\subsection{\texttt{response\_consistency}}

This is the global coefficient of variation across every intra-pump latency
in the entire session:

\begin{equation}
    \text{CV}_\text{global} = \frac{\sigma(\text{all\_latencies})}{\mu(\text{all\_latencies})}
\end{equation}

For the sample session this is $1.204$. Honestly, on its own this number is
a bit hard to interpret, because it lumps together motor jitter within a
single balloon and strategic tempo shifts between balloons. The decomposition
below is more informative.


\subsection{\texttt{within\_balloon\_consistency}}

For each balloon that has at least 3~pumps, we compute the CV of that balloon's
inter-pump intervals (again filtering out pauses over 2\,s), and then average
those CVs across all qualifying balloons:

\begin{lstlisting}[style=pythonstyle]
within_cvs = []
for balloon_events in balloons:
    ts = [e.timestamp for e in balloon_events if e.type == "pump"]
    if len(ts) >= 3:
        diffs = np.diff(ts)
        diffs = diffs[diffs < 2000.0]
        if len(diffs) >= 2 and np.mean(diffs) > 0:
            within_cvs.append(np.std(diffs) / np.mean(diffs))

within_balloon_cv = np.mean(within_cvs) if within_cvs else 0.0
\end{lstlisting}

Low values (below about $0.2$) point to steady, rhythmic pumping; anything
above $0.6$ or so starts to suggest hesitation or attentional lapses in the
middle of a balloon.


\subsection{\texttt{between\_balloon\_consistency}}

Here we look at the CV of \emph{pump counts} across collected balloons (falling
back to all balloons if fewer than 5 were collected):

\begin{lstlisting}[style=pythonstyle]
collected_counts, all_counts = [], []
for b in balloons:
    p = sum(1 for e in b if e.type == "pump")
    all_counts.append(p)
    terminal = next(
        (e.type for e in reversed(b)
         if e.type in ("collect","explode")), None)
    if terminal != "explode":
        collected_counts.append(p)

arr = np.array(collected_counts if len(collected_counts) >= 5
               else all_counts, dtype=np.float64)
between_cv = np.std(arr) / np.mean(arr)
\end{lstlisting}

\subsubsection*{Reading the two together}

\begin{table}[H]
\centering
\begin{tabular}{@{}lll@{}}
\toprule
\textbf{Within CV} & \textbf{Between CV} & \textbf{What it suggests} \\
\midrule
Low  & Low  & Very consistent---same strategy, steady hands \\
Low  & High & Adapts strategy across balloons, but executes each one cleanly \\
High & High & Erratic across the board \\
High & Low  & One fixed strategy, but shaky execution within each trial \\
\bottomrule
\end{tabular}
\end{table}


% ──────────────────────────────────────────────────────────────────────────
\section{Composite Metrics}
% ──────────────────────────────────────────────────────────────────────────

\subsection{\texttt{adaptive\_strategy\_score}}

This is meant to be a single summary number for ``behavioral intelligence''
across the whole task. It blends three equally weighted components---learning,
discrimination, and calibration---each of which can contribute up to
33.33~points:

\begin{equation}
\boxed{
\texttt{adaptive\_strategy\_score} =
\underbrace{\frac{\texttt{half\_split\_lr} + 1}{2} \times 33.33}_{\text{Learning}}
\;+\;
\underbrace{\texttt{color\_disc} \times 33.33}_{\text{Discrimination}}
\;+\;
\underbrace{\frac{\texttt{risk\_adj}}{100} \times 33.33}_{\text{Calibration}}
}
\end{equation}

\begin{lstlisting}[style=pythonstyle]
safe = lambda v: 0.0 if np.isnan(v) else v

adaptive_strategy_score = (
    (safe(half_split_lr) + 1) / 2 * 33.33
    + safe(color_discrimination) * 33.33
    + safe(risk_adjustment) / 100 * 33.33
)
adaptive_strategy_score = np.clip(adaptive_strategy_score, 0, 100)
\end{lstlisting}

We gave each component equal weight because, frankly, we do not have a strong
theoretical reason to privilege one over the others. In practice, scores
above $70$ tend to come from people who learn, differentiate, \emph{and}
calibrate well; scores below $40$ usually signal either a flat strategy,
poor color discrimination, or performance that actually deteriorated over time.

Sample session: $56.1$---decent discrimination pulled the score up, but
weak calibration (the purple under-pumping, mainly) kept it from going higher.


% ──────────────────────────────────────────────────────────────────────────
\section{Session Validation}
% ──────────────────────────────────────────────────────────────────────────

\subsection{\texttt{session\_valid}}

Before we score anything, the data goes through five sanity checks:

\begin{enumerate}
    \item \textbf{Minimum balloons.}  Fewer than 15 means we throw the session
          out entirely; 15--29 gets a warning.
    \item \textbf{Color balance.}  Each color should have around 10~balloons;
          if any color drops below~5 we flag it.
    \item \textbf{Timestamp monotonicity.}  If any $t_i < t_{i-1}$, something
          went wrong with the logging and the session is invalid.
    \item \textbf{Session speed.}  Completing 15 or more balloons in under 30
          seconds is suspiciously fast.
    \item \textbf{Pump uniformity.}  If the standard deviation of pump counts
          is below $0.5$ across 10 or more balloons, it looks like a bot or
          an auto-clicker was involved.
\end{enumerate}

\begin{lstlisting}[style=pythonstyle,caption={Bot-detection heuristic (Python).}]
pump_counts = [sum(1 for e in b if e.type == "pump")
               for b in balloons]
if len(pump_counts) >= 10 and float(np.std(pump_counts)) < 0.5:
    warnings.append(
        "Suspicious: nearly identical pump counts "
        "across all balloons")
\end{lstlisting}

\subsection{\texttt{session\_warnings}}

This is a list that collects messages from the validation step as well as from
any data-quality fallbacks that get triggered during scoring itself.


% ──────────────────────────────────────────────────────────────────────────
\section{Behavioral Profile (Narrative)}
% ──────────────────────────────────────────────────────────────────────────

Once all the numbers have been computed, we also generate a short narrative
profile. This is mostly for the participant-facing reflection screen.

\subsection{\texttt{risk\_style}}

Each participant gets assigned one of four archetypes based on straightforward
threshold logic:

\begin{table}[H]
\centering
\begin{tabular}{@{}lp{5.5cm}p{5cm}@{}}
\toprule
\textbf{Style} & \textbf{Trigger condition} & \textbf{Description} \\
\midrule
High-Stakes Risk Taker   & \texttt{rng\_normalized} $> 0.45$ & Pushes limits to the maximum \\
Conservative Safety-Seeker & \texttt{rng\_normalized} $< 0.10$ \textbf{and} \texttt{avg\_adj} $< 10$ & Prioritizes safety above all else \\
Strategic Risk Taker      & \texttt{risk\_sensitivity} $> 0.7$ & Takes calculated risks when the odds are favourable \\
Balanced Explorer         & default & A healthy mix of caution and exploration \\
\bottomrule
\end{tabular}
\end{table}

Sample session: \emph{High-Stakes Risk Taker}.


\subsection{\texttt{dominant\_traits}}

On top of the archetype, we attach additional trait labels, each gated by a
single threshold:

\begin{table}[H]
\centering
\begin{tabular}{@{}lp{7.5cm}@{}}
\toprule
\textbf{Trait} & \textbf{Condition} \\
\midrule
Highly Consistent       & within\_CV $< 0.2$ \textbf{and} between\_CV $< 0.4$ \\
Erratic (within-balloon)& within\_CV $> 0.6$ \\
Strategically Variable  & between\_CV $> 1.0$ \\
Adaptive Learner        & \texttt{half\_split\_lr} $> 0.1$ \\
Risk-Averse Learner     & \texttt{half\_split\_lr} $< -0.1$ \\
Impulsive               & \texttt{orange\_avg\_pumps} $> 4.0$ \\
Patient Optimizer       & \texttt{patience\_index} $> 40$ \\
\bottomrule
\end{tabular}
\end{table}

Sample session: \emph{Variable/Erratic}, \emph{Impulsive}.


% ──────────────────────────────────────────────────────────────────────────
\section{Metric Classification Table}
\label{sec:classification}
% ──────────────────────────────────────────────────────────────────────────

\begin{small}
\begin{longtable}{@{}lllc@{}}
\toprule
\textbf{Metric} & \textbf{Type} & \textbf{Data Source} & \textbf{Used in Clustering} \\
\midrule
\endhead
\texttt{average\_pumps\_adjusted}     & Descriptive & Collected only     & No \\
\texttt{avg\_pumps\_all\_balloons}    & Descriptive & All balloons       & No \\
\texttt{explosion\_rate}              & Outcome     & All balloons       & No \\
\texttt{mean\_latency\_between\_pumps}& Temporal    & All balloons       & No \\
\texttt{rng\_normalized\_pumps}       & Behavioral  & Collected only     & Yes \\
\texttt{impulsivity\_index}           & Behavioral  & Collected orange   & Yes \\
\texttt{patience\_index\_normalized}  & Behavioral  & Collected purple   & Yes \\
\texttt{orange\_avg\_pumps}           & Behavioral  & Collected orange   & No \\
\texttt{patience\_index}              & Behavioral  & Collected purple   & No \\
\texttt{learning\_rate}               & Adaptation  & Collected / color  & No \\
\texttt{half\_split\_learning\_rate}  & Adaptation  & Collected / color  & No \\
\texttt{color\_discrimination\_index} & Calibration & Collected / color  & No \\
\texttt{risk\_sensitivity}            & Calibration & Collected / color  & Yes \\
\texttt{risk\_adjustment\_score}      & Calibration & Collected / color  & No \\
\texttt{response\_consistency}        & Temporal    & All latencies      & No \\
\texttt{within\_balloon\_consistency} & Temporal    & Per-balloon lat.   & No \\
\texttt{between\_balloon\_consistency}& Strategic   & Collected counts   & No \\
\texttt{adaptive\_strategy\_score}    & Composite   & All collected      & No \\
\texttt{session\_valid}               & Validation  & All events         & No \\
\bottomrule
\end{longtable}
\end{small}


% ──────────────────────────────────────────────────────────────────────────
\section{K-Means Clustering Pipeline}
\label{sec:clustering}
% ──────────────────────────────────────────────────────────────────────────

\textbf{A word of caution before we begin.} Everything in this section comes
from a pilot sample of just $N = 12$ participants. We are aiming for 30--50
by the time data collection wraps up, and at that point the feature set, the
number of clusters, and the profile interpretations will almost certainly look
different. What follows is a snapshot of the pipeline as it stands now and
its preliminary output---not a final finding.

\subsection{Feature Horizon}

For this pilot run we narrowed things down to five features that seemed to
do the best job of separating participants given the small sample:

\begin{table}[H]
\centering
\begin{tabular}{@{}llp{7cm}@{}}
\toprule
\textbf{Feature} & \textbf{Source} & \textbf{What it captures} \\
\midrule
\texttt{dospert\_financial}           & DOSPERT-30 & Self-reported willingness to take financial risks \\
\texttt{bart\_risk\_sensitivity}      & BART       & Pearson $r$ between balloon capacity and pumping behavior \\
\texttt{bart\_patience\_normalized}   & BART       & How much of the purple (safe) balloon's capacity gets used, normalized to $[0,1]$ \\
\texttt{bart\_impulsivity\_index}     & BART       & Orange (dangerous) balloon pumping, normalized to $[0,1]$ \\
\texttt{bart\_rng\_normalized\_pumps} & BART       & Capacity-normalized pumping intention pooled across all colors \\
\bottomrule
\end{tabular}
\caption{Features used in the pilot clustering run. We expect this set to
grow once we have more participants.}
\end{table}


\subsection{Preprocessing}

All features are standardized to zero mean and unit variance before anything
else happens:

\begin{lstlisting}[style=pythonstyle,caption={Preprocessing (Python).}]
from sklearn.preprocessing import StandardScaler

# Drop participant_id, keep only clustering features
df_numeric = df_work[CONTINUOUS_COLS].copy()

scaler = StandardScaler()
df_scaled = pd.DataFrame(
    scaler.fit_transform(df_numeric),
    columns=df_numeric.columns,
    index=df_numeric.index,
)
\end{lstlisting}

We also do a quick correlation check and flag any pairs where $|r|$ exceeds
$0.70$, just to catch redundancy before it warps the distance metric.


\subsection{Choosing $k$: Elbow and Silhouette}

We sweep $k$ from 2 through 6 and evaluate each solution using inertia
(within-cluster sum of squares) alongside the silhouette score:

\begin{lstlisting}[style=pythonstyle,caption={$k$-selection loop (Python).}]
from sklearn.cluster import KMeans
from sklearn.metrics import silhouette_score

inertias, silhouettes = [], {}
for k in range(2, 7):
    km = KMeans(n_clusters=k, n_init=20, random_state=42,
                max_iter=2000)
    labels = km.fit_predict(X)
    inertias.append(km.inertia_)
    silhouettes[k] = silhouette_score(X, labels)

best_k = max(silhouettes, key=silhouettes.get)
\end{lstlisting}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{01_k_selection.png}
\caption{Elbow plot (left) and silhouette scores (right) for $k = 2 \ldots 6$.
The pilot data point toward $k = 2$, with a silhouette of $0.343$.  There is
no sharp bend in the elbow curve, but that is hardly surprising at $N = 12$---
you just do not have enough points to carve out more than two groups cleanly.}
\label{fig:k_selection}
\end{figure}


\subsection{Cluster Profiles}

Having settled on $k = 2$, we fit the final model and look at where each
cluster's centroid sits relative to the overall sample mean in $z$-score terms:

\begin{lstlisting}[style=pythonstyle,caption={Profile computation (Python).}]
km = KMeans(n_clusters=best_k, n_init=20, random_state=42)
labels = km.fit_predict(df_scaled.values)

centroids = df.groupby("cluster")[features].mean()
z_centroids = (centroids - df[features].mean()) / df[features].std()
\end{lstlisting}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{02_cluster_profiles.png}
\caption{Heatmap of cluster centroids as $z$-scores.  Cluster~0
(``risk-engaged'') lands above the mean on every feature; Cluster~1
(``risk-averse'') is the mirror image.  The biggest separator is
\texttt{bart\_patience\_normalized} ($z = \pm 0.82$), with
\texttt{bart\_rng\_normalized\_pumps} ($\pm 0.71$) close behind.}
\label{fig:profiles}
\end{figure}

Each cluster ended up with exactly 6 participants. The profiles are strikingly
symmetric---Cluster~0 sits above the global mean on every feature, Cluster~1
below it---which is pretty much what you would expect from a $k = 2$ solution
on a small, roughly Gaussian sample. The main thing we can take away at this
stage is that the split is being driven primarily by BART patience and overall
capacity usage, with DOSPERT financial risk tagging along in the same direction.


\subsection{PCA Projection}

To get a sense of how well the two clusters separate in a lower-dimensional
space, we projected the standardized features onto their first two principal
components:

\begin{lstlisting}[style=pythonstyle,caption={PCA projection (Python).}]
from sklearn.decomposition import PCA

pca = PCA(n_components=2)
X_2d = pca.fit_transform(df_scaled.values)
# PC1: 52.7% variance, PC2: 27.7% (80.4% total)
\end{lstlisting}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{03_pca_projection.png}
\caption{PCA projection of the 12 pilot participants.  PC1 (52.7\% of the
variance) pulls the two clusters apart along the horizontal axis.  PC2
(27.7\%) picks up the remaining within-cluster spread, driven largely by
\texttt{bart\_impulsivity\_index} (loading $= 0.778$).}
\label{fig:pca}
\end{figure}

The PCA loadings tell a story that is consistent with the heatmap:

\begin{table}[H]
\centering
\begin{tabular}{@{}lrr@{}}
\toprule
\textbf{Feature} & \textbf{PC1} & \textbf{PC2} \\
\midrule
\texttt{bart\_patience\_normalized}   & $+0.556$ & $-0.169$ \\
\texttt{bart\_rng\_normalized\_pumps} & $+0.542$ & $+0.196$ \\
\texttt{bart\_risk\_sensitivity}      & $+0.450$ & $-0.527$ \\
\texttt{dospert\_financial}           & $+0.406$ & $+0.225$ \\
\texttt{bart\_impulsivity\_index}     & $+0.172$ & $+0.778$ \\
\bottomrule
\end{tabular}
\caption{PCA loadings.  PC1 is essentially a general ``risk engagement'' axis---every
feature loads positively.  PC2 captures a contrast between impulsivity (positive loading)
and risk sensitivity (negative loading), which is an interesting wrinkle: some
participants score high on impulsivity without necessarily being risk-\emph{sensitive}.}
\end{table}

The first two components together account for $80.4\%$ of the total variance,
which is a reasonable amount for five input features.


\subsection{What We Expect to Change at Scale}

Once we reach 30--50 participants, several things will probably shift:

\begin{itemize}
    \item \textbf{More clusters.}  The $k = 2$ solution is almost certainly an
          artifact of the small sample. With more data we would hope to see at
          least 3 or 4 distinct personas emerge---separating, say, ``impulsive
          risk-taker'' from ``patient strategist'' from ``cautious across the
          board.''
    \item \textbf{A wider feature set.}  We plan to bring back
          \texttt{bart\_mean\_latency}, \texttt{bart\_between\_consistency}, and
          \texttt{bart\_adaptive\_strategy} once there are enough participants for
          those features to pull their weight. Additional DOSPERT subscales
          (health/safety, recreational, ethical, social) may also come into play.
    \item \textbf{Demographics.}  Binary and nominal variables like gender, dorm
          status, employment, and prior task experience are currently left out
          of the clustering, but we intend to test them either as candidate
          features or as post-hoc descriptors of the resulting groups.
    \item \textbf{Proper validation.}  At a larger $N$ we can run stability
          checks---bootstrap resampling, split-half consistency---and interpret
          silhouette scores with more confidence.
\end{itemize}


% ──────────────────────────────────────────────────────────────────────────
\section{Appendix: Sample Session Walkthrough}
% ──────────────────────────────────────────────────────────────────────────

Session~\texttt{6fe2f79a-0135-4850-9a5c-f0969be6088c}.

\begin{table}[H]
\centering\small
\begin{tabular}{@{}clccc@{}}
\toprule
\textbf{\#} & \textbf{Color} & \textbf{Pumps} & \textbf{Outcome} & \textbf{Notes} \\
\midrule
 1 & \textcolor{purpleballoon}{Purple} &  6 & Collected & \\
 2 & \textcolor{tealballoon}{Teal}     &  3 & Exploded  & \\
 3 & \textcolor{tealballoon}{Teal}     &  4 & Exploded  & \\
 4 & \textcolor{tealballoon}{Teal}     &  5 & Collected & \\
 5 & \textcolor{purpleballoon}{Purple} &  5 & Exploded  & \\
 6 & \textcolor{purpleballoon}{Purple} &  3 & Exploded  & \\
 7 & \textcolor{orangeballoon}{Orange} &  1 & Exploded  & first-pump pop \\
 8 & \textcolor{orangeballoon}{Orange} &  4 & Exploded  & \\
 9 & \textcolor{purpleballoon}{Purple} &  6 & Collected & \\
10 & \textcolor{orangeballoon}{Orange} &  5 & Exploded  & \\
11 & \textcolor{tealballoon}{Teal}     &  5 & Collected & \\
12 & \textcolor{purpleballoon}{Purple} & 11 & Collected & \\
13 & \textcolor{tealballoon}{Teal}     &  4 & Exploded  & \\
14 & \textcolor{purpleballoon}{Purple} &  7 & Collected & \\
15 & \textcolor{tealballoon}{Teal}     &  3 & Collected & \\
16 & \textcolor{purpleballoon}{Purple} & 21 & Collected & session high \\
17 & \textcolor{tealballoon}{Teal}     &  2 & Exploded  & \\
18 & \textcolor{orangeballoon}{Orange} &  2 & Exploded  & \\
19 & \textcolor{orangeballoon}{Orange} &  4 & Exploded  & \\
20 & \textcolor{purpleballoon}{Purple} & 15 & Exploded  & \\
21 & \textcolor{tealballoon}{Teal}     &  6 & Exploded  & \\
22 & \textcolor{orangeballoon}{Orange} &  4 & Exploded  & \\
23 & \textcolor{purpleballoon}{Purple} & 16 & Exploded  & \\
24 & \textcolor{orangeballoon}{Orange} &  4 & Exploded  & \\
25 & \textcolor{tealballoon}{Teal}     &  5 & Collected & \\
26 & \textcolor{purpleballoon}{Purple} &  9 & Exploded  & \\
27 & \textcolor{orangeballoon}{Orange} &  2 & Exploded  & \\
28 & \textcolor{orangeballoon}{Orange} &  2 & Exploded  & \\
29 & \textcolor{tealballoon}{Teal}     &  3 & Collected & \\
30 & \textcolor{orangeballoon}{Orange} &  1 & Exploded  & first-pump pop \\
\bottomrule
\end{tabular}
\caption{Complete balloon-by-balloon breakdown.}
\end{table}

\begin{table}[H]
\centering
\begin{tabular}{@{}lr@{}}
\toprule
\textbf{Metric} & \textbf{Value} \\
\midrule
\texttt{total\_balloons}               & 30 \\
\texttt{total\_pumps}                  & 168 \\
\texttt{total\_explosions}             & 20 \\
\texttt{total\_collections}            & 10 \\
\texttt{average\_pumps\_adjusted}      & 7.20 \\
\texttt{explosion\_rate}               & 0.667 \\
\texttt{mean\_latency\_between\_pumps} & 555.4\,ms \\
\texttt{learning\_rate}                & 0.059 \\
\texttt{risk\_adjustment\_score}       & 32.2 \\
\texttt{color\_discrimination\_index}  & 0.833 \\
\texttt{risk\_sensitivity}             & 0.678 \\
\texttt{adaptive\_strategy\_score}     & 56.1 \\
\texttt{response\_consistency}         & 1.204 \\
\texttt{patience\_index}               & 9.9 \\
\texttt{risk\_style}                   & High-Stakes Risk Taker \\
\texttt{dominant\_traits}              & Variable/Erratic, Impulsive \\
\bottomrule
\end{tabular}
\caption{Scored output for the sample session.}
\end{table}


% ──────────────────────────────────────────────────────────────────────────
\section*{References}
% ──────────────────────────────────────────────────────────────────────────

\begin{itemize}

    \item Blais, A.-R.\ \& Weber, E.U.\ (2006).
        A Domain-Specific Risk-Taking (DOSPERT) scale for adult populations.
        \textit{Judgment and Decision Making}, 1(2), 33--47.

    \item Din\c{c}, S.C.\ \& Yava\c{s} Tez, \"O.\ (2019).
        Alana \"ozg\"u risk alma \"ol\c{c}e\u{g}i --- k{\i}sa formu'nun
        (DOSPERT-30) T\"urk\c{c}eye uyarlama \c{c}al{\i}\c{s}mas{\i}
        [The adaptation study into Turkish of the Domain-Specific Risk-Taking
        Scale --- Short Form (DOSPERT-30)].
        \textit{Spor Bilimleri Dergisi}, 30(3), 107--120.
        \url{https://doi.org/10.17644/sbd.471304}

    \item Lejuez, C.W., Read, J.P., Kahler, C.W., et al.\ (2002).
        Evaluation of a behavioral measure of risk taking: The Balloon Analogue
        Risk Task (BART). \textit{Journal of Experimental Psychology: Applied},
        8(2), 75--84.

    \item Weber, E.U., Blais, A.-R., \& Betz, N.E.\ (2002).
        A domain-specific risk-attitude scale: Measuring risk perceptions and
        risk behaviors. \textit{Journal of Behavioral Decision Making}, 15(4),
        263--290.

\end{itemize}


\end{document}
